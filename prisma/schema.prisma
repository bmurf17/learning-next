generator client {
    provider        = "prisma-client-js"
}

datasource db {
    provider             = "mysql"
    url                  = env("DATABASE_URL")
    relationMode         = "prisma"
}

model Program {
    id          Int       @id @default(autoincrement())
    name        String
    description String
    image       String
}
